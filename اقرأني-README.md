# Royal Dental House — نظام إدارة العيادة (نسخة متكاملة)

## الجديد في هذه النسخة
- **عدد الغرف قابل للتعديل** من تبويب الإعدادات (مش لازم يكون غرفتين بالظبط).
- **يعمل أونلاين وأوفلاين**: البيانات بتتخزن مؤقتًا على الجهاز، ولما النت يرجع بتتزامن تلقائيًا مع باقي الأجهزة — مفيش فقدان بيانات لو النت قطع لحظيًا.
- **بحث سريع عن المريض** بدل النزول في قائمة طويلة (في الروشتات، الفواتير، والمواعيد).
- **طرق دفع أكتر**: كاش، فيزا، انستاباي، محفظة إلكترونية، تحويل بنكي.
- **تعديل بيانات المريض** بعد إضافته (اسم، هاتف، سن، عنوان، ملاحظات).
- **تعديل بيانات صنف في المخزون** بعد إضافته (اسم، فئة، وحدة، حد التنبيه، السعر).
- **زرار واتساب** بجانب كل موعد ومريض — بيفتح واتساب مباشرة برسالة تذكير أو تواصل جاهزة تقدر تعدلها قبل الإرسال.
- **حسابات مستخدمين وصلاحيات**: كل موظف بيسجل دخول بحسابه، والصفحات اللي بيشوفها تختلف حسب صفته.

## ملاحظة صراحة عن سبب عدم ثبات البيانات قبل كده
لو كنت بتجرب النسخة اللي في المحادثة نفسها (المعاينة التفاعلية) وحسيت إن البيانات بتضيع، السبب إنها نسخة تجريبية للمعاينة السريعة داخل شات كلود بس، مش الأداة اللي هتستخدمها فعليًا في العيادة. **الملف المرفق هنا (index.html) هو النسخة الحقيقية** المتصلة بقاعدة بياناتك على Firebase، وهي دي اللي بتحفظ كل حاجة بشكل دائم ومتزامن بين الأجهزة.

## حسابات المستخدمين — إزاي تشتغل
عند فتح البرنامج لأول مرة، هيُطلب منك تسجيل دخول أو إنشاء حساب:
1. **أول حساب تعمله (أنت)**: اختار الصفة **"شريك"** — ده هيديك صلاحية كاملة على كل الأقسام بما فيها الحسابات والمرتبات وإدارة المستخدمين.
2. **باقي الموظفين**: كل واحد يعمل حساب بنفسه (بريد إلكتروني + كلمة مرور) ويختار صفته:
   - **استقبال**: المواعيد، المرضى، الفواتير
   - **تمريض**: المواعيد، المرضى، المخزون
   - **طبيب (غير شريك)**: المواعيد، المرضى، الروشتات، الفواتير، المخزون
   - **شريك**: كل شيء + الحسابات والمرتبات + إدارة صلاحيات باقي المستخدمين
3. من تبويب **"المستخدمون"** (يظهر للشركاء فقط) تقدر تغيّر صفة أي حساب في أي وقت.

**صراحةً**: أي شخص يعمل حساب جديد بيختار صفته بنفسه وقت التسجيل (مفيش موافقة تلقائية منك)، فالنظام ده تنظيمي لفريق موثوق فيه، مش حماية ضد شخص متعمد يكذب في صفته وقت التسجيل — لو حصل، تقدر تصحّح صفته فورًا من تبويب المستخدمين. لو عايز تأمين أقوى من كده (دعوة كل موظف بنفسك بدل ما يسجل هو)، ده ممكن كخطوة تالية.

## خطوة مهمة: قفل قاعدة البيانات
1. روح console.firebase.google.com → مشروعك → Build → Firestore Database → Rules
2. امسح اللي موجود، والصق بدله محتوى ملف firestore.rules المرفق هنا
3. اضغط Publish

هتلاقي كمان في نفس المكان (Build → Authentication) لازم تفعّل طريقة الدخول: من تبويب Sign-in method فعّل **Email/Password**.

## النشر + تحويله لـ APK
1. ارفع الملفات (index.html, manifest.json, service-worker.js, icons/) على استضافة مجانية زي GitHub Pages أو Netlify.
2. روح pwabuilder.com، الصق رابط موقعك، اختار Android، Download → هتاخد ملف APK جاهز للتثبيت.
3. أو ببساطة افتح رابط الموقع من موبايل الأندرويد واضغط "إضافة إلى الشاشة الرئيسية".

كل نسخة (APK أو من المتصفح) هتشتغل على نفس البيانات المشتركة تلقائيًا لأنها كلها متصلة بنفس قاعدة Firebase.
